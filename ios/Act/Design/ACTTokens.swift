import SwiftUI

/// Reserved namespace for the design-system color tokens.
/// Full token generation (oklch-based palette + semantic aliases) is implemented
/// in the `design-tokens` todo. Hex constants that must exist outside this file
/// are restricted to the SwiftLint-approved set: #000000, #0A0A0A, #141414, #1C1C1E.
enum ACTTokens {}
