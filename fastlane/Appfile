# Bundle ID for the iOS app. Must match ios/Act.xcodeproj's PRODUCT_BUNDLE_IDENTIFIER.
app_identifier "com.act.coach"

# Apple ID and team_id are supplied via the App Store Connect API key
# (key_id + issuer_id + key_filepath) at upload time, so they are not
# pinned here. See fastlane/Fastfile :beta lane.
